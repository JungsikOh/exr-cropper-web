{
  "name": "exr-cropper-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --open /index.source.html",
    "build": "tsc && vite build && node scripts/sync-pages-build.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bb-studio/exr": "^0.1.1",
    "jszip": "^3.10.1",
    "lucide": "^0.475.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.2.0",
    "vitest": "^3.1.1"
  }
}
